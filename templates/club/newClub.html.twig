{% extends "base.html.twig" %}
{% block title %}New Club
{% endblock %}

{% block body %}
	<h1>Create new Club</h1>

	{{ form_start(formClub) }}
	{{ form_widget(formClub) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(formClub) }}

	<a href={{path('app_club_index')}}>Cancel</a>
{% endblock %}
